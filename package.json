{
  "name": "tely",
  "private": true,
  "scripts": {
    "native": "yarn && cd packages/native && yarn dev",
    "web": "yarn && cd packages/web && yarn dev",
    "lint": "eslint . --cache --cache-location=tmp/.eslintcache",
    "typecheck": "tsc"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/react": "^18.0.15",
    "@types/react-native": "^0.69.2",
    "@wyattades/eslint-config": "^0.11.0",
    "eslint": "^8.19.0",
    "prettier": "^2.7.1",
    "turbo": "^1.3.1",
    "typescript": "^4.7.4"
  }
}
